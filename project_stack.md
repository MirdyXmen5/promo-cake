🏗️ 1. Backend (Серверная часть)
Мозг твоего приложения. Мы выбрали этот стек за скорость разработки и надежность.
Язык программирования: Python 3.9+ (Современный, читаемый, мощный).
Веб-фреймворк: Flask
Почему: Легковесный микро-фреймворк. Идеален для микросервисов и MVP. Не перегружен лишним, как Django.
WSGI Сервер (Production): Gunicorn
Зачем: Flask сам по себе (через app.run) не держит нагрузку. Gunicorn запускает приложение в несколько потоков (воркеров), чтобы обслуживать 50 директоров одновременно без зависаний.

🗄️ 2. Database (База Данных)
Память проекта. Мы ушли от файловой базы к промышленному стандарту.
СУБД: PostgreSQL 15
Почему: Золотой стандарт индустрии. Надежная, поддерживает одновременную запись (MVCC), не "ломается", если 20 человек одновременно сканируют QR.
ORM (Работа с данными): SQLAlchemy (через Flask-SQLAlchemy)
Зачем: Позволяет работать с базой как с объектами Python (классы User, PromoCode, ScanLog). Защищает от SQL-инъекций (взлома через ввод).
Драйвер БД: psycopg2-binary (мост между Python и Postgres).

🎨 3. Frontend (Клиентская часть)
То, что видят директора. Ориентировано на мобильные телефоны (Mobile First).
HTML5 / CSS3: Стандартная верстка.
UI Фреймворк: Bootstrap 5
Почему: Адаптивность "из коробки". Красивые кнопки, карточки и уведомления, которые отлично выглядят и на iPhone, и на Android.
Шаблонизатор: Jinja2
Зачем: Генерирует HTML страницы на сервере, вставляя туда данные (имя юзера, сумму выигрыша, цветные плашки).
QR-Сканер: html5-qrcode (JavaScript Library)
Фишка: Позволяет сканировать коды прямо в браузере через камеру телефона, без установки мобильного приложения.

🛡️ 4. Security & Auth (Безопасность)
Аутентификация: Flask-Login (Управление сессиями пользователей, вход/выход, защита страниц @login_required).
Хеширование паролей: Werkzeug Security
Алгоритм: pbkdf2:sha256. Мы не храним пароли в открытом виде, только их зашифрованные слепки.

🐳 5. DevOps & Infrastructure (Инфраструктура)
То, на чем это все крутится.
Контейнеризация: Docker
Зачем: Упаковали приложение и базу в контейнеры. Гарантирует, что "работает на моем компе = работает на сервере".
Оркестрация: Docker Compose
Зачем: Одной командой (docker-compose up) поднимает и связывает сеть между Flask и PostgreSQL.
Web Server / Reverse Proxy (В планах на деплой): Nginx
Зачем: Будет стоять перед Gunicorn. Раздает статику, защищает сервер и, главное, обеспечивает HTTPS (SSL).
SSL Сертификаты: Let's Encrypt (Certbot) (Для работы камеры в браузере по HTTPS).